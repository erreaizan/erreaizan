pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo '🛠️  Building...'
            }
        }

        stage('Test') {
            steps {
                echo '🧪 Testing...'
            }
        }

        stage('Deploy') {
            steps {
                echo '🚀 Deploying...'
                sh 'docker rm -f test-jenkins || true'
                sh ' docker build -t test test-jenkins .'
                sh 'docker run -d -p 8080:8080 --name test-jenkins test-jenkins'
            }
        }
    }
}

